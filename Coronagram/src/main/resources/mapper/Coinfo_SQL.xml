<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="coinfo">
	<insert id="coinfoAdd" parameterType="hashmap">
		INSERT INTO INFO(INFO_TITLE, INFO_SUBHD, CON, REP_IMG)
		VALUES (M1_SEQ.NEXTVAL, #{info_title}, #{info_subhd}, #{con}, #{imgFile})
	</insert>
	
<!-- 
	<select id="getInfolist" parameterType="hashmap" resultType="hashmap">
		SELECT INFO_TITLE, INFO_SUBHD, TO_CHAR(DT, 'YYYY-MM-DD') AS DT, REP_IMG
		FROM INFO
		WHERE INFO_NO = #{no}
	</select>
	
	<select id="getInfopage" parameterType="hashmap" resultType="hashmap">
		SELECT INFO_TITLE, INFO_SUBHD, TO_CHAR(DT, 'YYYY-MM-DD') AS DT, REP_IMG, CON 
		FROM INFO
		WHERE INFO_NO = #{no}
	</select>
 -->
</mapper>