<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Qna">
	<select id="getQnaList" parameterType="hashmap" resultType="hashmap">
		SELECT QUESTION.Q_NO, QUESTION.TITLE, QUESTION.DT, QUESTION.CON
		FROM (	
				SELECT Q_NO, TITLE, TO_CHAR(DT,'yyyy-mm-dd') AS DT, CON,
				ROW_NUMBER() OVER(ORDER BY Q_NO DESC) AS RNUM
				FROM QUESTION
				WHERE DEL = 0
			) QUESTION
			WHERE QUESTION.RNUM BETWEEN #{startCnt} AND #{endCnt}
	</select>
</mapper>